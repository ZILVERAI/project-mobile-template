const baseConfig = require('./app.json');

module.exports = ({ config }) => {
  return {
    ...baseConfig.expo,
    ...config,
    extra: {
      ...config.extra,
      // Your proxy URL
      devServerUrl: process.env.DEV_SERVER_URL || 'https://your-domain.com',
    },
    // Attempt to set the dev client URL scheme
    scheme: 'frontend',
  };
};
